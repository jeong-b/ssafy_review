const a = 1;
const b = 1.2;
const c = "a";
const d = "abc";
const e = true;
const f = false;
console.log(typeof a);
console.log(typeof b);
console.log(typeof c);
console.log(typeof d);
console.log(typeof e);
console.log(typeof f);

/* 
1. 변수 선언시 선언자를 반드시 붙여라
2. 선언자는 총 세 종류가 있다.
  var   : 안쓴다. 가끔 선배들이 쓰는데 뭐라하지 마라!
  let   : 자주 안쓴다. for문에 i정도에 쓴다 
  const : 이거 써라! 근데 작동 안하면 let 써라!
*/

/*
숫자는 다 number다.
int , float, double등 은 존재하지 않는다.
문자는 다 string이다.
char 역시 존재하지 않는다.
그래서 '' 대신 ""만 쓴다.
boolean은 소문자로만 쓴다.
*/